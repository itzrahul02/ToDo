// export {TodoContext, TodoProvider, useTodo} from "./TodoContext"
function details(user){
    console.log(`Hi my name is ${user.name} and Id is ${user.id}`);
}
user={
    name:"Rahul",
    id:2064
};
details(user) 